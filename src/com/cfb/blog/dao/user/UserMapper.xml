<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cfb.blog.dao.user.UserMapper">
	<select id="selectUserForm" parameterType="Map" 
			resultType="com.cfb.blog.entity.user.UserForm">
		SELECT USER_NAME AS userName FROM user WHERE USER_NAME = 'system'
	</select>
</mapper>